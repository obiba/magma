description = 'Magma Hibernate Datasource'

dependencies {
  //compile project(':magma-api')
  compile project(':magma-hibernate')
  compile project(':magma-hibernate:magma-hibernate-common')
  compile(libraries.json)
//  compile(libraries.jta)
//  compile(libraries.obiba_core)
//  compile(libraries.hibernate_core)
//  compile(libraries.hibernate_annotations)
//  compile(libraries.spring_beans)

  testCompile project(':magma-data-generator')
  testCompile(libraries.mysql_connector_java)
  testCompile(libraries.javassist)
  testCompile(libraries.hibernate_ehcache)
}
