description = 'Magma CSV Datasource'

dependencies {
  compile project(':magma-api')
  compile(libraries.opencsv)

  testCompile project(':magma-test')
  testCompile(libraries.commons_io)
  testCompile(libraries.obiba_core)
}
